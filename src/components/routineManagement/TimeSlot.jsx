// This component has been refactored into a modular structure
// All functionality is now in the TimeSlot folder with separate components
import TimeSlot from './TimeSlot/index';

export default TimeSlot;
